<!-- 
  Filename: scripts.ejs
  Student's Name: Justin Amaral
  StudentID: 301230988
  Date: 28-05-2023
-->

<script src="/bootstrap/js/bootstrap.bundle.min.js"></script>
<!-- <script>
  document.querySelectorAll(".update-contact").forEach((item) => {
    item.addEventListener("click", function (event) {
      let contactId = event.target.getAttribute("data-contactId");

      // PUT Request - replace the URL with your own API URL
      fetch("/dashboard/contacts?contactId=" + contactId, {
        method: "PUT",
        headers: {
          "Content-Type": "application/json",
        },
        body: JSON.stringify({
          // TODO: update the fields here as per your needs.
        }),
      })
        .then((response) => response.json())
        .then((data) => console.log("Success:", data))
        .catch((error) => console.error("Error:", error));
    });
  });

  document.querySelectorAll(".delete-contact").forEach((item) => {
    item.addEventListener("click", function (event) {
      let contactId = event.target.getAttribute("data-contactId");

      // TODO: Implement the DELETE Request here
      // DELETE Request - replace the URL with your own API URL
      fetch("http://localhost:3000/dashboard/contacts/" + contactId, {
        method: "DELETE",
      })
        .then((response) => {
          if (!response.ok) {
            return response.json(); //If response not ok, return the error message
          }
          console.log("Success.");
        })
        .catch((error) => console.error("Error:", error));
    });
  });
</script> -->
